# TODO

Maintainer-internal engineering intents that should version with the code. User-facing bugs and feature requests belong in GitHub issues; items that would survive the Zametek.Graphs.Avalonia spin-out belong in [src/Zametek.Graphs.Avalonia/TODO.md](src/Zametek.Graphs.Avalonia/TODO.md). Date entries when added; delete them when done.

- [ ] **Remove System.Reactive once DynamicData and Dock go Rx-free** *(2026-08-02, last checked 2026-08-15)* - ReactiveUI 24 no longer uses System.Reactive, but removal is pointless while other packages still hard-depend on it, so the assembly ships regardless. As of the last check, Dock is mid-split: 12.1.0.1 introduced the suffixed System.Reactive lane (`Dock.Model.ReactiveUI.Reactive`, paired with `ReactiveUI.Reactive`) and documents the unsuffixed family as the ReactiveUI.Primitives lane (`docfx/articles/dock-reactiveui.md` in the Dock repo); the shipped unsuffixed `Dock.Model.ReactiveUI` 12.1.0.1 still declares `System.Reactive` 7.0.0, but Dock master has already removed the reference, so the next unsuffixed release should flip the Dock gate. `DynamicData` is the remaining hard blocker in released packages (9.4.33 and 9.5.0-preview.15 both declare `System.Reactive` 6.1.0; no suffixed twin exists), but its direction is confirmed: PR [reactivemarbles/DynamicData#1116](https://github.com/reactivemarbles/DynamicData/pull/1116) migrates DynamicData to ReactiveUI.Primitives, and the maintainer plans to ship it in a 10.0.0-preview (package naming - a suffixed convention like ReactiveUI/Dock vs explicit dual names - was still undecided when checked), so the Rx-free DynamicData arrives as a MAJOR (10.x) bump, previews first. Watch: confirm the next unsuffixed Dock release drops `System.Reactive`, then watch for a stable DynamicData 10.x without it. Bump caution: this solution is deliberately in the unsuffixed/Primitives lane - never adopt a `.Reactive`-suffixed package, the two families have distinct type identities and must not be mixed. Once both gates pass, examine migrating the solution's own Rx usage to `ReactiveUI.Primitives`. Known gaps to solve at that point: no `FromEventPattern` (two uses, the collection-changed bridges in the effort tracking manager), no `Subject`/`BehaviorSubject` (Primitives has different "Signal" abstractions), and no `Observable.Create` (`MuteWhile` is built on it). Breadcrumbs: comments above the `System.Reactive` reference in `Zametek.Graphs.Avalonia.csproj` and the Dock/DynamicData block in `Zametek.ViewModel.ProjectPlan.csproj`; the `ObservableExtensions.ObserveOn(ISequencer)` bridge is the seam from which to start unwinding.

- [ ] **Spin out Zametek.Graphs.Avalonia into its own repository** *(2026-08-02)* - the library is already framework-decoupled with its own README; the checklist lives in [src/Zametek.Graphs.Avalonia/TODO.md](src/Zametek.Graphs.Avalonia/TODO.md) so it travels with the folder.

- [ ] **Formal drag-and-drop from charts** *(2026-08-02, consolidated from a code TODO)* - `ScottPlotUserControl.CheckPointerDrag` detects the click-vs-drag threshold but only tracks state; the inline comment marks where `DragDrop.DoDragDropAsync` would start a real DND operation (e.g. dragging a chart image into another app).

- [ ] **Reduce UI-thread reads of locked view-model getters** *(2026-08-04)* - the dotnet-trace profile of an edit burst showed several seconds of Monitor contention: bindings re-reading locked getters while the background compile cascade held the locks. Most of it vanished with the IsBusy fix; if input hitches reappear under heavy background activity, prefer lock-free snapshots (volatile fields or immutable models) for hot bound properties.

- [ ] **Consider reporting the Dock float/re-dock behaviour upstream** *(2026-08-02, optional)* - under Dock 12.1, dropping a floating tool back into the layout materialises the destination view while the closed float window's visual tree is still assembled (12.0.0.2 did not surface this). Our crash was ultimately the app's own shared-control-as-Content pattern - now fixed by construction in `ScottPlotUserControl` - but a minimal repro (any dockable binding a shared control instance as Content) may be worth filing against Dock so the cross-window materialisation ordering gets a look.

- [ ] **Rule-6 surgery: stop notifying and calling foreign components while holding locks** *(2026-08-17)* - two dump-proven deadlocks (both captured 2026-08-17 during scenario loads) came from lock-order cycles among `CoreViewModel.m_Lock`, `ProjectScenarioDisplaySettingsViewModel.m_Lock`, `GanttActivitySelectorViewModel.m_Lock` and ReactiveUI's internal `ExpressionChainSink` gates. The point fixes already landed: the lock-free getter sweep (ARCHITECTURE §7 rule 9), hoisting the display-settings core callbacks (`SetIsProjectScenarioUpdated`/`IsReadyToCompile`) out of all the setter lock blocks, and ref-counted revising guards in the Gantt/earned-value selectors (a shared bool let one reviser's `finally` clear the other's guard mid-revision across threads). The REMAINING call-out-under-lock sites are hardening, not live bugs - every remaining edge is one-way with the point fixes in place - and form the deferred surgery, in descending value: (1) `CoreViewModel.ProcessProjectScenario`/`ResetProjectScenario` hold `m_Lock` across the entire load fan-out: raising settings setters, raising `IsReadyToReviseTrackers` (whose `Scheduler.CurrentThread` subscriptions synchronously run selector revisions ON the raising thread, taking selector locks under the core lock - the Core→Selector edge in the first dump), and calling `DisplaySettingsViewModel.SetValues` (the Core→DS edge in the second dump); surgery = split each into a state phase under the lock and a fan-out phase after release, collecting pending raises/signals under the lock and emitting them outside - §7 rule 7 (handlers re-read live state; payloads are stale snapshots) is what makes raise-after-release semantically safe, and the `Begin`/`EndBulkUpdate` window wraps both phases so suppression semantics survive. (2) `ProjectScenarioDisplaySettingsViewModel.SetValues` assigns ~30 raising setters under the DS lock, so every synchronous observer (ReactiveUI sink gates) runs under it; surgery = set fields and collect changed property names under the lock, raise them all after release (this also collapses the setters' reentrant locking). (3) The four selector view models mutate `ObservableCollection`s inside their locked `Set*` methods, so `CollectionChanged` handlers (which raise, and in Gantt/EV write through to DS/Core) execute under the selector lock; surgery = detach the handler around locked mutations and do one manual refresh+raise after release, or compute the change set under the lock and apply it outside. (4) Chart-manager plot-model setters (earned-value/Gantt/resource charts) and `ResourceTrackerSetViewModel.RefreshIndex` raise inside their locks - trivial set-under-lock/raise-after splits. (5) Document the global lock-acquisition DAG in ARCHITECTURE §7 (Manager→Core→DisplaySettings, Core→Selector, node/tracker VMs as leaves; verify core never calls back into the scenario manager under its lock) so future cross-VM calls have an order to conform to. Adjacent smells noted while investigating: `GanttChartShowConnections`/`EarnedValueShowResources` are plain `List<int>` mutated by UI handlers without the DS lock while `SetValues` seeds them under it (torn-read risk), and DS setters mark the scenario dirty even when `RaiseAndSetIfChanged` is a no-op write. Related: the 2026-08-04 contention entry above - the getter sweep already delivered its "lock-free snapshots for hot bound properties" suggestion for the selector/tracker getters. **DIRECTION TO EXPLORE - delete locks rather than order them** *(2026-08-17)*: the reactive cascade itself is not the problem and does not need rewriting - in all three dumps the dependency graph behaved correctly and the failures were in the locking wrapped around it - but the current design is safe only by discipline, not by construction, because three properties combine badly: one lock per view model with cross-view-model calls made freely in both directions; synchronous notification (`PropertyChanged`/`CollectionChanged`) that re-enters arbitrary foreign code holding locks we neither own nor can inspect (ReactiveUI `ExpressionChainSink` gates, DynamicData, Dock); and `Scheduler.CurrentThread` subscriptions that run on whichever thread happened to raise, so "which thread executes this" is not statically knowable. The lock graph is therefore not fully knowable either, and a deadlock stays one refactor away no matter how carefully rules 6 and 9 are applied. Target end state: view models are UI-thread affine and hold NO locks at all, while background work (parse, compile, metrics) operates on immutable snapshots - as the compiler already does when it clones the graph - and returns a single result applied on the UI thread; with no locks there is no lock order to get wrong and rule 9 becomes unnecessary rather than merely enforced. Pragmatic 80% if full affinity proves too invasive on the load path: exactly ONE lock at the Core/model boundary and none anywhere else, which (combined with rules 6 and 9) removes every cycle among our own locks by construction. The path is incremental and mostly already walked - the getter sweep permanently killed the first class, the setter hoists killed the live instance of the second, and once rule 6 holds everywhere most remaining view-model locks will be found to guard nothing but brief field writes that only ever happen on one thread, at which point they can be DELETED rather than ordered (strictly better than maintaining a documented lock DAG forever, and it subsumes item 5 above). Add mechanical enforcement instead of vigilance: `System.Threading.Lock` exposes `IsHeldByCurrentThread`, so debug-only assertions at the raise/callback boundaries turn a latent deadlock into a loud test failure rather than a dump weeks later. Finally, the "third dump" (`zametek-deadlock-2.dmp`, 2026-08-17 - NOT a deadlock: no thread blocked on any lock, UI thread idle, while a scenario-load worker span inside `PriorityListResourceScheduler.CalculateResourceSchedules` with nothing ever scheduled onto any of the 14 resource builders) is no longer parked: the 2026-08-18 ClrMD analysis root-caused the spin as a torn `HashSet` clone poisoning the scheduler's `Contains` gate (ARCHITECTURE §7 rule 10 tells the full story and names the ProjectPlan-side race that tore it), the scheduler itself now detects the dead state upstream (Zametek.Maths.Graphs 3.2.0 reports compilation error C0020 instead of spinning, and `Compile` takes a cancellation token), and the app-side compile watchdog (`CompilationTimeoutMilliseconds`, 2026-08-19) bounds anything the scheduler cannot prove dead. The remaining structural item from that paragraph - run the compile outside the lock against a snapshot - is Phase 2 of the compile-on-snapshots entry below.

- [ ] **Compile on snapshots - Phase 2 of the live-activity data-race fix** *(2026-08-19)* - Phase 1 (landed 2026-08-19) removed the proven corruption vector: the per-activity `ResourceSettings`/`WorkStreamSettings` subscriptions that deferred `Clear()`+`UnionWith` mutations of live target sets to the UI thread, where they raced the scenario-load compile's clone step (ARCHITECTURE §7 rule 10 documents the race, the dump evidence and the new synchronous fan-out contract). Phase 2 kills the remaining class: a grid edit committed while a previously-armed taskpool compile is still in flight mutates the live activity the compiler is cloning (input lane), and the compiler's own in-place write-back of results (`ResetResourceState`, both `CalculateCriticalPath` passes, `AssignResourceDependencies`, `UpdateActivitySuccessors` all mutate the live `ManagedActivityViewModel`s) races UI binding reads and `DeepCopy()` saves/clipboard copies (output lane). Design: restructure `RunCompile` into snapshot -> compile -> publish. *Snapshot*: clone every activity once (the same `CloneObject()` the compiler already uses) into a private input set at a point where mutators are provably quiescent. *Compile*: run a throwaway `VertexGraphCompiler` over the clones - the fresh-compiler-from-models pattern already exists in `GraphCompilationService`; the persistent `m_VertexGraphCompiler` keeps its live graph for the between-compile services (`GetNextActivityId`, dependency cycle checks, `TransitiveReduction`), which all run under `m_Lock`. *Publish*: copy the schedule outputs (early/late times, `AllocatedToResources`, resource dependencies, successors) back onto the live view models in one explicit place before setting `GraphCompilation` - the fiddly part, a faithful field-for-field replacement for the compiler's implicit write-back, and the part that closes the output lane. Open design decision (deliberately deferred until Phase 1 is GUI-verified): the quiescence mechanism for the brief snapshot/publish steps - (a) a narrow leaf data-lock taken by each mutator write and by snapshot/publish only (mechanical, ~15 sites, thread-agnostic so the load path and CLI need no special casing, never held across raises or the compile itself, so deadlock-free by construction; UI blocks only for the microseconds-to-milliseconds of a clone loop) versus (b) posting snapshot/publish to the UI thread (zero new locks, aligns with the rule-6 "delete locks" end state, but forces `RunCompile` into an async pipeline that reshapes the load path, the `RunAutoCompile` callers and the CLI). Bonus once the compile runs on private clones: `m_Lock` need not be held across the compile itself, which delivers the "compile outside the lock" item from the rule-6 entry and stops UI bindings contending with multi-second compiles. Sibling pattern noted but deliberately not part of Phase 1: `ManagedResourceViewModel` still absorbs `WorkStreamSettings` through a MainThreadScheduler-deferred subscription, but its mutations stay inside manager-owned models that reach the core only as `DeepCopy` pushes on the UI thread, so there is no corruption path into the compiler - at most a stale-ordering wrinkle; fold it into Phase 2 for uniformity. Test debt: the ViewModel test project has no fixture that constructs a full `CoreViewModel`, so Phase 1's ordering contract (target sets reconciled before `IsReadyToCompile` raises) is pinned only by the CLI's end-to-end scenario loads; build that fixture as part of Phase 2 so the ordering pin, a snapshot-isolation test (mutating a view model mid-compile neither corrupts nor alters the in-flight compile) and a reflection-poisoned-`HashSet` regression test can land together (the Zametek.Maths.Graphs session has the poisoning technique).
